#include <iostream>
#include <algorithm>

using namespace std;

int main() {
	int n, m;
	int a[1000] = { 0 };

	cin >> n >> m;

	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	int lp(0), rp(0);
	// left pointer, right pointer

	while (rp < n - 1) {
		if (a[rp] < m) {

		}
		else {

		}
	}

	return 0;
}
